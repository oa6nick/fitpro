# Gradle 8.11 не запускается на Java 25 — использовать JDK 17–21
# (Android Studio сам берёт свой JBR; для CLI выставить JAVA_HOME на него, см. README).
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
