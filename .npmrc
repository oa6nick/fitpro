# Монорепо: drizzle-orm тянет optional-peer (react-native через op-sqlite/expo-sqlite),
# который конфликтует с react@18 у client. Эти peer'ы опциональны и не нужны нам
# (используем pglite/node-postgres), поэтому игнорируем peer-резолюцию.
legacy-peer-deps=true
